สร้างเกมส์ ค้นหาหมวกของฉัน
1.กำหนดจุดเริ่มต้น
2.ใช้คำสั่ง Random ในการกำหนดจุดเริ่มต้น, จุดเกิดหลุม, จุดที่พบหมวก
3. import {createInterface} from "node:readline"; //นำเข้าฟังก์ชันรับค่าจาก Terminal 
const prompt = require('prompt-sync')({ sigint: true });//ประกาศตัวแรก promt เพื่อรดึงโมดูลสำหรับรับค่าจากผู้ใช้ (User Input) บน Terminal ใน Node.js มาใช้งาน
4.